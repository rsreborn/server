import { Npc } from "../npc";
import { Player } from "../player";

export interface Chunk { // 8 x 8 more tities
    // objects: RSObject[];
    npcs: number[];
    players: number[];
    // triggers:
}
